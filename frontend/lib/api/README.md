API client modules (client.ts, auth.ts, reports.ts, projects.ts, users.ts, dashboard.ts) will
be added here once we start wiring up individual features, using NEXT_PUBLIC_API_URL from
.env.local as the base URL.
